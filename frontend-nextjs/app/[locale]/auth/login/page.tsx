import { PublicPlaceholder } from '@/components/public-placeholder';

export default function LoginPage() {
  return (
    <PublicPlaceholder
      title="Login flow removed in public repository"
      description="The public repository keeps the localized routing and page structure, but the authentication implementation is intentionally omitted."
    />
  );
}
