import { PublicPlaceholder } from '@/components/public-placeholder';

export default function ProfilePage() {
  return (
    <PublicPlaceholder
      title="Profile area omitted in public repository"
      description="The original profile management flow is not included in this public showcase version."
    />
  );
}
