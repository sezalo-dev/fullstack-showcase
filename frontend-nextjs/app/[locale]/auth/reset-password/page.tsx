import { PublicPlaceholder } from '@/components/public-placeholder';

export default function ResetPasswordPage() {
  return (
    <PublicPlaceholder
      title="Password reset flow removed"
      description="This page remains as a placeholder to show routing structure, while the implementation is intentionally excluded from the public repository."
    />
  );
}
