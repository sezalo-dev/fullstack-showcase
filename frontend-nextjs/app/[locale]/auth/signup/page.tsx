import { PublicPlaceholder } from '@/components/public-placeholder';

export default function SignupPage() {
  return (
    <PublicPlaceholder
      title="Signup flow removed in public repository"
      description="The registration flow is represented as a placeholder so the app structure stays visible without exposing the internal implementation."
    />
  );
}
