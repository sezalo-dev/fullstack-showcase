import { PublicPlaceholder } from '@/components/public-placeholder';

export default function EditListingPage() {
  return (
    <PublicPlaceholder
      title="Edit flow removed in public repository"
      description="The edit route remains as a placeholder while the internal form and persistence logic are omitted."
    />
  );
}
