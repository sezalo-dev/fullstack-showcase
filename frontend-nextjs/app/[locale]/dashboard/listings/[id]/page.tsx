import { PublicPlaceholder } from '@/components/public-placeholder';

export default function DashboardListingDetailPage() {
  return (
    <PublicPlaceholder
      title="Private listing detail omitted"
      description="Detailed dashboard listing management is intentionally excluded from the public repository."
    />
  );
}
