import { PublicPlaceholder } from '@/components/public-placeholder';

export default function NewListingPage() {
  return (
    <PublicPlaceholder
      title="Listing creation flow removed"
      description="The route is preserved for structural clarity, while the original creation workflow is not included in the public showcase."
    />
  );
}
