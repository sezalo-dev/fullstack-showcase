import { PublicPlaceholder } from '@/components/public-placeholder';

export default function DashboardListingsPage() {
  return (
    <PublicPlaceholder
      title="Listing management omitted"
      description="CRUD logic for private dashboard listing management was removed from the public repository."
    />
  );
}
