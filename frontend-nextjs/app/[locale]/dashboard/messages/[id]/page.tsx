import { PublicPlaceholder } from '@/components/public-placeholder';

export default function DashboardMessageDetailPage() {
  return (
    <PublicPlaceholder
      title="Conversation detail removed"
      description="The conversation UI is represented by a placeholder in the public showcase version."
    />
  );
}
