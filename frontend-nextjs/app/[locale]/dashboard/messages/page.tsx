import { PublicPlaceholder } from '@/components/public-placeholder';

export default function DashboardMessagesPage() {
  return (
    <PublicPlaceholder
      title="Messaging inbox removed"
      description="The messaging dashboard remains visible as part of the route map, but its implementation is not included in the public repository."
    />
  );
}
