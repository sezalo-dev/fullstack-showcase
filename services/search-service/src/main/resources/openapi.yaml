openapi: 3.1.0
info:
  title: Search Service API
  version: 0.1.0
servers:
  - url: http://localhost:8082
paths:
  /api/v1/public/search:
    get:
      summary: Search listings
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: OK
